#include<bits/stdc++.h>
using namespace std;
using ll=long long ;

void solve()
{
    vector<int> dis;
    string s;
    cin>>s;
    int n=s.length();
    for(int i=0;i<n;i++){
        if(s[i]=='0') s.push_back(i);
    }
    
}

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    solve();
}
