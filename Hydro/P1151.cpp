#include<bits/stdc++.h>
using namespace std;
using ll=long long;

void solve()
{
    int n;
    cin>>n;
    vector<int> s,t;
    int l,r,ans;
    ans=-1e9;
    for(int i=1;i<=n;i++){
        int x;
        cin>>x;
        int a,b;
        if(x>0) a=1;
        else a=-1;
        
        if(x==0){

        }
        else s.push_back(x);
    }
}

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    solve();
}